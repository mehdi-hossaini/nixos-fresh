{
  "$comment": "Inventory of the tools available on this machine, for an agent working over Bash. Derived from modules/nixos/packages.nix + modules/home/default.nix — those files are the source of truth; this one is a reading aid. Deliberately carries no version numbers: run `<tool> --version` instead, which is always current. Update after changing the package lists.",
  "$schema_notes": [
    "This file is checked mechanically by ~/.claude/check-conventions.sh — keep these contracts true or the check fails.",
    "tools[].commands — the executables the entry puts on PATH. Omitted means `[name]`. An empty array means the entry ships no binary under its own name (a bundle or a library).",
    "tools[].package — the nixpkgs attribute or home-manager program name. Must appear in modules/nixos/packages.nix or modules/home/default.nix. null means it is not separately declared (base system).",
    "not_installed[].names — every command the entry covers, one per element. This is the machine-readable field; there is no `name` here on purpose, so the list cannot drift from its prose.",
    "tools[].agent_unsafe — command prefixes that hang a session with no terminal. Optional; present only on the interactive entries. modules/nixos/claude.nix maps each to a `Bash(<prefix> *)` deny rule, so adding an interactive tool here denies it too — the prose in `purpose` is the reading aid, this array is what the wall is built from. A prefix rather than a bare name because `zellij attach` is unsafe while `zellij action` and `zellij run` are not."
  ],
  "machine": {
    "hostname": "nixos-machine",
    "arch": "x86_64-linux",
    "cpu_threads": 12,
    "ram_gb": 14,
    "shell": "fish (default login shell); bash is present for scripts",
    "config_repo": "/etc/nixos (git, branch main; owned by the user via tmpfiles — no sudo needed to edit)",
    "package_sources": [
      "/run/current-system/sw/bin — system packages (modules/nixos/packages.nix)",
      "/etc/profiles/per-user/mehti/bin — home-manager programs (modules/home/default.nix)"
    ],
    "env": {
      "EDITOR": "code --wait — interactive sessions only. Agent sessions pin JJ_EDITOR/GIT_EDITOR/EDITOR/VISUAL to `false` via ~/.claude/settings.json, so a command that would open an editor fails fast instead of hanging on a GUI window. That guard does not cover jj's diff editor (jj split/diffedit/resolve), which is a builtin TUI.",
      "RUSTC_WRAPPER": "sccache",
      "CARGO_BUILD_JOBS": "6 (derived from machine.buildCores — see modules/nixos/machine.nix)",
      "CLAUDE_CONFIG_DIR": "/home/mehti/.claude"
    },
    "notes": [
      "Impermanence is enabled: only declared/persisted paths survive a reboot. Do not leave state in the home root.",
      "Build parallelism is derived from machine.threads and machine.memoryGiB (modules/nixos/machine.nix), not hand-set — this host lands on max-jobs 2 x cores 6 because RAM is the binding constraint. Do not raise it by hand; change machine.memoryGiB and let the formula follow.",
      "Installing software imperatively does not work here. Add the package to modules/nixos/packages.nix and run `nh os switch /etc/nixos`, or use `nix shell nixpkgs#<pkg>` for a one-off."
    ]
  },
  "tools": [
    {
      "name": "rg",
      "package": "ripgrep",
      "category": "search",
      "purpose": "Fast recursive regex search over file contents.",
      "prefer_over": [
        "grep -r"
      ]
    },
    {
      "name": "fd",
      "package": "fd",
      "category": "search",
      "purpose": "Fast file/directory search by name.",
      "prefer_over": [
        "find"
      ]
    },
    {
      "name": "ast-grep",
      "package": "ast-grep",
      "category": "search",
      "purpose": "Structural search and rewrite over a language's AST — use for code patterns that regex handles badly.",
      "notes": "Invoke as `ast-grep`, never as `sg`. `sg` on this machine resolves to /run/wrappers/bin/sg (util-linux setgid), not ast-grep."
    },
    {
      "name": "jq",
      "package": "jq",
      "category": "search",
      "purpose": "JSON query and transformation."
    },
    {
      "name": "git",
      "package": "git",
      "category": "vcs",
      "purpose": "Version control. Kept because jj uses it as its backend and gh speaks it.",
      "config": {
        "init.defaultBranch": "main",
        "pull.rebase": true,
        "push.autoSetupRemote": true,
        "gc.auto": 0,
        "safe.directory": [
          "/etc/nixos"
        ]
      }
    },
    {
      "name": "jj",
      "package": "jujutsu",
      "category": "vcs",
      "purpose": "Jujutsu VCS over the git backend — colocated repos, no staging area, `jj undo` on any operation.",
      "notes": "Bare `jj` is configured to show the log. `git.colocate` and `git.track-default-bookmark-on-clone` default to true; `git.auto-local-bookmark` no longer exists. No version is named here on purpose: check-conventions.sh asserts all four claims against `jj util config-schema` and `jj config get`, so they go red when one stops being true rather than when jj is upgraded."
    },
    {
      "name": "jjui",
      "package": "jjui",
      "category": "vcs",
      "purpose": "TUI over jj's operation log. Interactive — do not launch from an agent shell.",
      "agent_unsafe": [
        "jjui"
      ]
    },
    {
      "name": "gh",
      "package": "gh",
      "category": "vcs",
      "purpose": "GitHub CLI — PRs, issues, `gh api`.",
      "notes": "Auth is per-user state; check `gh auth status` before assuming it is logged in."
    },
    {
      "name": "nix",
      "package": "nix",
      "category": "nix",
      "purpose": "Flakes-enabled Nix. `nix shell nixpkgs#<pkg>` for a throwaway tool, `nix build`, `nix flake`."
    },
    {
      "name": "nh",
      "package": "nh",
      "category": "nix",
      "purpose": "NixOS rebuild wrapper: `nh os switch /etc/nixos`, `nh os boot`, `nh clean`.",
      "notes": "nh declines to run as root — run it as the user. /etc/nixos is user-owned, so that works without sudo; the git safe.directory entry is a leftover guard, no longer load-bearing."
    },
    {
      "name": "nixd",
      "package": "nixd",
      "category": "nix",
      "purpose": "Nix language server (used by the editor, not usually called directly)."
    },
    {
      "name": "statix",
      "package": "statix",
      "category": "nix",
      "purpose": "Nix linter. `statix check <path>`, `statix fix`."
    },
    {
      "name": "nixfmt",
      "package": "nixfmt",
      "category": "nix",
      "purpose": "Official Nix formatter. Reachable directly, but in /etc/nixos prefer `nix fmt`, which runs it under treefmt with this repo's exclusions applied."
    },
    {
      "name": "nix-index",
      "package": "nix-index",
      "commands": [
        "nix-index",
        "nix-locate"
      ],
      "category": "nix",
      "purpose": "`nix-locate bin/<name>` says which package ships a file — the lookup step behind \"a missing tool is a decision\", without guessing attribute names.",
      "notes": "The database is prebuilt and declared (nix-index-database, wired in modules/nixos/home-manager.nix). Never run `nix-index` to build one: it writes to ~/.cache/nix-index, which impermanence does not persist, so the work is lost at reboot and shadows the declared copy until then."
    },
    {
      "name": "devenv",
      "package": "devenv",
      "category": "dev-env",
      "purpose": "Per-project development environments (devenv.nix). This is where language toolchains live — including Rust via languages.rust, and mold via languages.rust.mold.enable."
    },
    {
      "name": "direnv",
      "package": "direnv",
      "category": "dev-env",
      "purpose": "Auto-loads a project's devenv shell on cd. Fish integration is enabled. Declared as `programs.direnv` in modules/home/default.nix, with `nix-direnv.enable = true` alongside it.",
      "notes": "A non-interactive agent shell may not have the project env loaded — run `direnv exec . <cmd>` if a project tool appears missing."
    },
    {
      "name": "uv",
      "package": "uv",
      "commands": [
        "uv",
        "uvx"
      ],
      "category": "dev-env",
      "purpose": "Python package/project manager. `uv run`, `uv sync`, and `uvx <cli>` for one-off Python CLIs.",
      "notes": "There is no system-wide python3 on this machine — uv is the only way to get a Python interpreter."
    },
    {
      "name": "sccache",
      "package": "sccache",
      "category": "dev-env",
      "purpose": "Compiler cache. Wired in globally via RUSTC_WRAPPER so devenv shells inherit it.",
      "notes": "`sccache --show-stats` to confirm cache hits. A cache hit is the cheapest way to survive the RAM ceiling."
    },
    {
      "name": "shellcheck",
      "package": "shellcheck",
      "category": "quality",
      "purpose": "Shell script linter. Run over any sh/bash script you write here."
    },
    {
      "name": "shfmt",
      "package": "shfmt",
      "category": "quality",
      "purpose": "Shell formatter — the other half of shellcheck. `shfmt -d <file>` shows the diff, `shfmt -w` applies it."
    },
    {
      "name": "deadnix",
      "package": "deadnix",
      "category": "nix",
      "purpose": "Finds unused bindings and function arguments in .nix files. Run it as `deadnix . -e ./hosts`.",
      "notes": "Third of three passes that do not overlap: nixfmt formats, statix lints idioms, deadnix finds what nothing uses. Two traps. `--exclude` is variadic and swallows the positional argument, so `deadnix -e ./hosts` scans nothing at all and reports success — give the path first. And `--edit` rewrites files with no regard for which are generated: it stripped an unused `pkgs` from hosts/*/hardware-configuration.nix, an edit that file forbids and that nixos-generate-config would undo. Read the findings and fix them by hand."
    },
    {
      "name": "moreutils",
      "package": "moreutils",
      "commands": [
        "chronic",
        "combine",
        "errno",
        "ifdata",
        "ifne",
        "isutf8",
        "lckdo",
        "mispipe",
        "pee",
        "sponge",
        "ts",
        "vidir",
        "vipe",
        "zrun"
      ],
      "category": "cli",
      "purpose": "Small shell utilities. `sponge` is the one that matters: `jq '…' f | sponge f` edits a file in place, where `jq '…' f > f` truncates it before jq reads it.",
      "notes": "The package also ships a `parallel` that is NOT GNU parallel and takes different flags — it is left out of `commands` here on purpose, so nothing in this inventory points an agent at it. `vidir` and `vipe` open $EDITOR: agent-unsafe, see the editor guard."
    },
    {
      "name": "yq",
      "package": "yq",
      "commands": [
        "yq",
        "tomlq",
        "xq"
      ],
      "category": "search",
      "purpose": "jq's query language over YAML (`yq`), TOML (`tomlq`) and XML (`xq`). Use instead of sed for structured config — a workflow file, a devenv.yaml.",
      "notes": "This is the python wrapper (kislyuk), deliberately not `yq-go`: it takes jq syntax, so there is no second query language on this machine. A `yq` example found online may be written for yq-go and will not parse."
    },
    {
      "name": "file",
      "package": "file",
      "category": "cli",
      "purpose": "Identify an unknown blob by content rather than extension."
    },
    {
      "name": "gitleaks",
      "package": "gitleaks",
      "category": "quality",
      "purpose": "Secret scanning. `gitleaks detect` / `gitleaks git` before pushing.",
      "notes": "This config repo is public — scan before committing anything that touches identity or credentials."
    },
    {
      "name": "bat",
      "package": "bat",
      "category": "cli",
      "purpose": "Pager/cat with syntax highlighting. Use `bat -pp` for plain, non-paged output in scripts."
    },
    {
      "name": "eza",
      "package": "eza",
      "category": "cli",
      "purpose": "Modern ls replacement (`eza -la`, `eza --tree`)."
    },
    {
      "name": "fzf",
      "package": "fzf",
      "category": "cli",
      "purpose": "Fuzzy finder. Interactive — needs a TTY; not useful non-interactively.",
      "agent_unsafe": [
        "fzf"
      ]
    },
    {
      "name": "btop",
      "package": "btop",
      "category": "cli",
      "purpose": "Resource monitor. Interactive TUI — do not launch from an agent shell.",
      "agent_unsafe": [
        "btop"
      ]
    },
    {
      "name": "zellij",
      "package": "zellij",
      "category": "terminal",
      "purpose": "Terminal multiplexer — the tabs Alacritty deliberately lacks. Can be driven non-interactively via `zellij action` / `zellij run`.",
      "agent_unsafe": [
        "zellij attach"
      ]
    },
    {
      "name": "alacritty",
      "package": "alacritty",
      "category": "terminal",
      "purpose": "GPU terminal emulator. GUI."
    },
    {
      "name": "code",
      "package": "vscode",
      "category": "editor",
      "purpose": "VS Code. Also the configured EDITOR (`code --wait`) for git and jj in interactive sessions.",
      "notes": "Agent sessions override the editor variables to `false` (see machine.env.EDITOR), so an editor-opening command errors instead of blocking on a GUI window. jj's diff editor is separate and still agent-unsafe: avoid jj split/diffedit/resolve and any -i flag."
    },
    {
      "name": "claude",
      "package": "claude-code",
      "category": "agent",
      "purpose": "Claude Code CLI.",
      "notes": "CLAUDE_CONFIG_DIR points at /home/mehti/.claude so its state lands in a persisted directory under impermanence."
    },
    {
      "name": "brave-origin",
      "package": "brave-origin",
      "category": "browser",
      "purpose": "Browser. GUI. Binary is `brave-origin`, not `brave`."
    },
    {
      "name": "prismlauncher",
      "package": "prismlauncher",
      "category": "games",
      "purpose": "Minecraft launcher. GUI.",
      "notes": "PrismLauncher-Cracked fork (github:Diegiwg/PrismLauncher-Cracked), overlaid to replace the official nixpkgs package — supports offline accounts only. The fork exists to bypass purchase verification, so a real Microsoft account is unsupported and risks violating Mojang/Microsoft ToS."
    },
    {
      "name": "fish",
      "package": "fish",
      "category": "shell",
      "purpose": "Default interactive shell.",
      "notes": "Fish is not POSIX. `&&`/`||` work, but `$(...)` subshell quirks, `export`, and array syntax differ — write scripts for bash and run them with `bash script.sh`."
    },
    {
      "name": "coreutils-and-friends",
      "package": null,
      "commands": [
        "bash",
        "sed",
        "awk",
        "find",
        "xargs",
        "grep",
        "diff",
        "patch",
        "tar",
        "gzip",
        "less",
        "man",
        "curl",
        "ssh",
        "rsync",
        "sudo",
        "systemctl"
      ],
      "category": "shell",
      "purpose": "Base-system commands, always present. `curl` lives here — there is no wget.",
      "notes": "Not a nixpkgs attribute of its own, hence package: null. The bundle name is not itself a command; the `commands` array is what exists on PATH."
    }
  ],
  "not_installed": [
    {
      "names": [
        "python",
        "python3"
      ],
      "workaround": "Use `uv run` / `uvx`. No system interpreter exists."
    },
    {
      "names": [
        "node",
        "npm"
      ],
      "workaround": "Per-project via devenv (`languages.javascript`), or `nix shell nixpkgs#nodejs`."
    },
    {
      "names": [
        "cargo",
        "rustc"
      ],
      "workaround": "Deliberately absent system-wide so it cannot shadow project toolchains. Use devenv's `languages.rust`."
    },
    {
      "names": [
        "gcc",
        "make"
      ],
      "workaround": "Not on PATH outside a nix shell. Use `nix shell nixpkgs#gcc nixpkgs#gnumake` or a devenv shell."
    },
    {
      "names": [
        "docker",
        "podman"
      ],
      "workaround": "Not enabled on this host."
    },
    {
      "names": [
        "wget"
      ],
      "workaround": "curl is installed; use `curl -LO`."
    },
    {
      "names": [
        "unzip"
      ],
      "workaround": "`nix shell nixpkgs#unzip`, or `bsdtar`/`tar` where the format allows."
    }
  ],
  "conventions": [
    "Never install with a package manager — edit modules/nixos/packages.nix (system) or modules/home/default.nix (user), then `nh os switch /etc/nixos`.",
    "For a tool needed once, `nix shell nixpkgs#<pkg> -c <cmd>` leaves no trace.",
    "In /etc/nixos: `nix fmt` formats the tree, `nix flake check` gates it. Formatting is treefmt's alone, configured once in treefmt.nix — do not invoke nixfmt or shfmt directly there, or the repo's exclusions are skipped. The check runs formatting, deadnix and shellcheck, plus evaluation of every host. There is no automatic pre-commit hook and there cannot be: jj has no hook support and bypasses .git/hooks entirely, so the gate is invoked, not triggered.",
    "Two things `nix flake check` does not cover. statix is not gated — it reports about twenty pre-existing findings, nearly all `repeated_keys`, where it wants attribute paths merged and this config writes them flat with a comment above each; run `statix check .` by hand and judge. And installer.sh is exempt from shfmt: it has never been formatted, needs 256 lines changed at any indent width, and partitions disks and shreds an encryption keyfile, so reformatting it is a deliberate decision rather than a side effect. shellcheck still covers it.",
    "hosts/*/hardware-configuration.nix is generated by nixos-generate-config and excluded from every nix linter — it carries a real unused argument whose only fix the file forbids, a permanent false positive. Running deadnix by hand: the path comes first, `deadnix . -e ./hosts`, because --exclude is variadic and swallows the positional, so `deadnix -e ./hosts` silently scans nothing and exits 0.",
    "`nh` and `git` run as the user, never as root. /etc/nixos is already user-owned, so edits need no sudo."
  ]
}
